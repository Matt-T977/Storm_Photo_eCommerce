class Product {
  constructor(id, name, type, resolution, description, price) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.resolution = resolution;
    this.description = description;
    this.price = price;
  }
}

export default Product;
