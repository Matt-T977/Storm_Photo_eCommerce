class Product {
  constructor(id, imageURI, name, type, resolution, description, price) {
    this.id = id;
    this.imageURI = imageURI;
    this.name = name;
    this.type = type;
    this.resolution = resolution;
    this.description = description;
    this.price = price;
  }
}

export default Product;
